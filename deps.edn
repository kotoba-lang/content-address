{:paths ["src"]
 ;; No dependencies on purpose. Addressing is arithmetic over bytes; the
 ;; libraries that own the layers around it (kotoba-protocol, chain,
 ;; tech-ipfs-specs-ipns, tech-ipfs-specs-http-gateway) depend on this
 ;; direction being one-way — an application composes them, and none of
 ;; them has to know that an archive exists.
 :deps {
        ;; kotoba.bytes replaced UTF-8 .getBytes / String. here (2026-09-24). An explicit
        ;; floor: tools.deps takes the NEWEST sha it is shown.
        io.github.kotoba-lang/bytes {:git/sha "d5259f35ab7c4f5a66e63ca29ed4dcf7ba3f1ac5"}
        ;; kotoba.io replaced clojure.java.io here (2026-09-24). An explicit floor:
        ;; kotoba.io carries clojure.java.io's file/as-file/as-relative-path/
        ;; make-parents/delete-file from 98045c7, and tools.deps takes the NEWEST
        ;; sha it is shown.
        io.github.kotoba-lang/io {:git/sha "1b60ca825c1e705bd195f857ca4549133653a9f3"}
        ;; kotoba.lang.edn replaced clojure.edn here (2026-09-23). An explicit floor:
        ;; tools.deps takes the NEWEST sha it is shown, and kbb resolves git deps
        ;; only from nbb.edn, so every project file this repo has names it.
        io.github.kotoba-lang/edn {:git/sha "79dba32c8892058842c57783f06a2dfe4601aeb0"}
       ;; kotoba.pprint replaced clojure.pprint here (2026-09-24): the same
        ;; bytes (cljs.pprint transcribed). An explicit floor; tools.deps takes the
        ;; NEWEST sha it is shown.
        io.github.kotoba-lang/pprint {:git/sha "5c9d4aea71d6caac529fffcf361aba0f87ee485e"}
        ;; kotoba.lang.text, not clojure.string. Pinned as an explicit floor:
        ;; tools.deps takes the NEWEST sha it is shown, so without a floor here
        ;; this repo silently rides whatever a sibling happens to name.
        io.github.kotoba-lang/text {:git/sha "73bdb13ae7a3d004b44bca08be03a3191157a38f"}
       }
 ;; There is deliberately NO :test alias here. The tests are ClojureScript
 ;; (`test/run_tests.cljk`, run under nbb — the runtime the appview build
 ;; steps actually use). A `kbb -M:test` that found zero tests and
 ;; exited 0 would be the failure this workspace keeps finding: a check
 ;; that could not run returning the same value as a check that passed.
 ;;
 ;;   kbb --backend sci --classpath src:test test/run_tests.cljk
 ;;
 ;; The compiler is dev-only and must not reach a consumer: `:kir` compiles
 ;; kotoba/address_core.kotoba into the artifact this library ships. What a
 ;; consumer gets is the artifact plus kotoba.kir, the interpreter.
 :aliases
 {:kir {:extra-paths ["tools"]
        :extra-deps {io.github.kotoba-lang/amu
                     {:git/url "https://github.com/kotoba-lang/amu.git"
                      :git/sha "b88cb6c0f276ab8f98bd2b62adeef0ae5ec7ffcd"}}
        :main-opts ["-m" "emit-kir"]}
  ;; Also dev-only: the fleet's naming plane. One IPNS name over a catalog
  ;; of addressed appviews, rather than one key per app — see the tool's ns
  ;; docstring. Publishing a name is an operator action, not something an
  ;; appview build step does, so this is not a consumer dependency either.
  :catalog {:extra-paths ["tools"]
            :extra-deps {io.github.kotoba-lang/tech-ipfs-specs-ipns
                         {:git/url "https://github.com/kotoba-lang/tech-ipfs-specs-ipns.git"
                          :git/sha "bca7ca43265a1c50d93e4b678c94989d15a29b51"}
                         io.github.kotoba-lang/io-libp2p-specs-kad-dht
                         {:git/url "https://github.com/kotoba-lang/io-libp2p-specs-kad-dht.git"
                          :git/sha "cf46f49a6abb12165b0be16509c8c6be967a6526"}}
            :main-opts ["-m" "publish-catalog"]}}}
